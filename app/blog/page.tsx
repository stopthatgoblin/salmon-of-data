import posts from '@/data/posts.json';
export const metadata={title:'The notebook'};
const date=(d:string)=>new Date(d+'T12:00:00Z').toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric',timeZone:'UTC'});
export default function Blog(){return <main className="wrap journal-page"><div className="journal-heading"><p className="eyebrow">The notebook</p><h1>Thinking behind<br/><em>the numbers.</em></h1><p>Notes on the hypotheses, the evidence, and what changes along the way.</p></div><div className="post-list">{posts.length?posts.map((p,i)=><article key={p.slug} className="post-row"><div className="post-meta"><span className="eyebrow">{p.tag}</span><time dateTime={p.date}>{date(p.date)}</time><span>{p.readingMinutes} min read</span></div><a href={'/blog/'+p.slug+'/'}><span className="post-number">{String(i+1).padStart(2,'0')}</span><h2>{p.title}</h2><p>{p.excerpt}</p><span className="text-link">Read the post <span>→</span></span></a></article>):<p className="empty-posts">New notes will appear here as the trackers develop.</p>}</div></main>}
